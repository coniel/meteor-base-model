{
  "name": "meteor-base-model",
  "version": "0.4.2",
  "description": "A model for all other models to inherit from",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist/** && babel lib --out-dir dist",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/coniel/meteor-base-model.git"
  },
  "keywords": [
    "meteor",
    "base",
    "model"
  ],
  "author": "Oscar Coniel",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/coniel/meteor-base-model/issues"
  },
  "homepage": "https://github.com/coniel/meteor-base-model#readme",
  "dependencies": {
    "simpl-schema": "^0.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-core": "^6.9.1",
    "babel-preset-es2015": "^6.13.2",
    "babel-preset-stage-0": "^6.5.0"
  }
}